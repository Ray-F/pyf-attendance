{
  "name": "pyf-attendance",
  "version": "1.1.8",
  "description": "a MERN stack application",
  "main": "index.js",
  "repository": "git@github.com:Ray-F/pyf-attendance.git",
  "author": "Raymond Feng <rf.raymondfeng@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "yarn run dev",
    "dev": "concurrently \"cd client && yarn run start\" \"cd server && yarn run start\"",
    "init": "concurrently \"cd client && yarn install\" \"cd server && yarn install\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
